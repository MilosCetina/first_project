import 'package:flutter/material.dart';

class DocumentDisplayPage extends StatelessWidget {
  const DocumentDisplayPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
